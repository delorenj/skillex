# Optional 1Password environment materialization.
# Add active assignments only when this project needs them, for example:
# API_KEY=op://your-vault/your-item/credential
#
# A comment-only file is an intentional opt-out: the mise enter hook leaves
# an existing .env byte-for-byte unchanged.
