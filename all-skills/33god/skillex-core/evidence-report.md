---
skill_name: skillex-core
generated: 2026-04-29T00:00:00Z
forge_tier: Deep
t2_future_count: 0
---

# Evidence Report — skillex-core

## Generation Metadata

| Field | Value |
|-------|-------|
| Skill | `skillex-core` v0.1.1 |
| Generated | 2026-04-29 |
| Generated by | Jarad DeLorenzo |
| Forge tier | Deep |
| Source repo | `delorenj/skillex` (local: `/home/delorenj/code/skillex`) |
| Source commit | `8d3670249e7cf8bf5c364995baf8720ef51dcc8f` |
| Source ref | `HEAD` (no git tags exist on this repo yet) |
| Compilation mode | Tightened spec (Tier-1 SKILL.md + minimal references; tessl/skill-check deferred) |

## Tool Versions

| Tool | Version |
|------|---------|
| ast-grep | 0.38.4 |
| qmd | operational (registry-only, no temporal/docs collections) |
| gh | 2.46.0 |
| ccc | not available (alias collision: `ccc` resolves to `code2prompt`, not cocoindex-code) |
| skf | unknown (no VERSION file in `_bmad/skf/`) |

## Extraction Summary

| Metric | Value |
|--------|-------|
| Files scanned | 5 |
| Public exports | 31 |
| Confidence: T1 (AST) | 31 |
| Confidence: T1-low (source) | 0 |
| Confidence: T2 (QMD enrichment) | 0 |
| Confidence: T3 (external docs) | 0 |
| Co-import patterns detected | 2 |
| Scripts inventory | 0 (out of scope) |
| Assets inventory | 0 (out of scope) |

## Validation Results

| Validator | Status |
|-----------|--------|
| Schema validation | [DEFERRED — tightened-spec mode; run `npx skill-check check skills/skillex-core` post-export] |
| Body validation | [DEFERRED — same] |
| Content quality (tessl) | [DEFERRED — run `npx -y tessl skill review skills/skillex-core` post-export] |
| Provenance citation density | PASS — every Tier-1 section has at least one `[AST:...]` citation; provenance map covers 31/31 exports |
| Description sanitization | PASS — 0 substitutions needed (no `<` or `>` in description) |

## Warnings

1. **Version drift between manifest and runtime constant.** `pyproject.toml [project] version = "0.1.1"` (used by the brief) vs `src/skillex/__init__.py:3 __version__ = "0.1.0"`. One-patch drift. Recommend bumping `__init__.py` to match `pyproject.toml` next commit.

2. **Layer inversion at `core/activator.py:18`.** `core` imports from `adapters.base` (excluded from this skill, see `skillex-core-activator`). This means `skillex-core` is not a strictly self-contained module — `activator.py` requires the `adapters` registry. The exclusion in this skill's scope is correct (activator gets its own skill), but consumers of `skillex-core` should know the dependency exists when using `LinkOp` against an activation plan.

3. **Private-symbol leak across module boundary.** `commands/status.py:11` imports the leading-underscore `_skillex_owned_links_in` from `core.activator`. Out of scope for this skill (lives in activator), but worth flagging for the activator skill's evidence report.

4. **Empty repository history.** Zero issues, zero PRs, zero releases on `delorenj/skillex` at compile time. No CHANGELOG.md. T2 enrichment was therefore skipped (no temporal data to index into QMD). Re-running create-skill after the project accumulates GitHub activity will produce richer T2 annotations.

5. **`ccc` not actually installed.** A zsh alias points `ccc` to `code2prompt` (different tool). cocoindex-code is not present, so semantic pre-ranking was unavailable. Forge+ tier features inactive; Deep tier proceeded with ast-grep + gh + qmd.

## Override Decisions

1. **Entry-point validation override (`__init__.py`-based public API rule).** `src/skillex/__init__.py` re-exports only `__version__`. Per the workflow's section §4b validation, this would mark all 31 extracted symbols as "internal — exclude from metadata.json exports". Overridden: skillex consumers import directly from `skillex.core.X` per project convention (verified at `commands/pack.py:11-21`, `adapters/base.py:14`, `adapters/claude.py:13`). All 31 module-level symbols in `core/*.py` recorded as part of the documented public API.

## Auto-Decisions

No auto-decisions — workflow ran interactively (or all gates had no match to auto-resolve).

## Skipped Phases (Tightened-Spec Mode)

The user selected the tightened-spec compilation path during the pilot run (option 2). The following standard phases were skipped or deferred:

- **Step 6 — Validate (`npx skill-check check`, `npx -y tessl skill review`):** deferred to manual run post-export.
- **Tier 2 — Full Type Definitions (`references/types.md`) and Full Integration Patterns (`references/integration.md`):** consolidated into the single `references/api-reference.md` covering all 31 exports.
- **Section 4b (Migration & Deprecation Warnings):** omitted — empty repo has no T2-future annotations to surface.
- **Section 7b (Scripts & Assets):** omitted — empty inventories.

These can be backfilled by re-running create-skill in standard mode against the same brief, or by running `/skf-test-skill` against the exported skill to surface any gaps.

## Next Steps

1. Run `/skf-test-skill skillex-core` to validate cognitive completeness.
2. Run `npx skill-check check skills/skillex-core` for schema/body validation.
3. Run `npx -y tessl skill review skills/skillex-core` for content quality scoring.
4. If pilot output is acceptable, run `/skf-create-skill --batch` again (continuing from index 1) to compile `skillex-adapters` and `skillex-core-activator` with full or tightened spec.
